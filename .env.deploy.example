# Copie este arquivo para .env.deploy e preencha os valores.
# Não commite .env.deploy (ele já está no .gitignore).

FTP_HOST=147.79.84.227
FTP_USER=u315410518.mundolepet.com.br
FTP_PASSWORD=sua_senha_ftp
FTP_PORT=21

# Pasta pública do site no servidor (onde ficam index.php e build/).
# Para sua estrutura atual, use:
FTP_REMOTE_PATH=/public_html/site

# Se definir, o deploy tambem envia app/, routes/, config/, bootstrap/, resources/views/ e artisan
# para o servidor (para atualizar a pagina Site e Google e outras alteracoes em PHP).
# Use o caminho da raiz do Laravel no servidor.
# Para sua estrutura atual, backend e público estão na mesma pasta:
# BACKEND_REMOTE_PATH=/public_html/site
